<!--
  After you run `flutter create .` (see README "Setup"), open the
  generated android/app/src/main/AndroidManifest.xml and add the
  permissions below inside the <manifest> tag, above <application>.
-->

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.CAMERA" />

<!-- Android 13+ granular media permissions -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

<!-- Android 12 and below -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />

<!--
  If your backend runs on plain http:// (no TLS) on your local network,
  e.g. http://192.168.1.20:5009, Android blocks cleartext traffic by
  default from API 28+. Add this to the <application> tag while
  developing:

    android:usesCleartextTraffic="true"

  For a real deployment, put the backend behind HTTPS instead (see the
  backend README) and remove this.
-->
